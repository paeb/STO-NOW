import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class PickaxeImage here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class PickaxeImage extends MapElement
{
    /**
     * Act - do whatever the PickaxeImage wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public PickaxeImage() {
        GreenfootImage image = getImage();
        image.scale(75, 75);
        //image.rotate(180);
    }   
    public void act() 
    {
        // Add your action code here.
    }    
}
