import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class FarmButton here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class FarmButton extends MapElement
{
    /**
     * Act - do whatever the FarmButton wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public FarmButton() {
        GreenfootImage image = getImage();
        image.scale(100, 50);
    }
    public void act() 
    {
        // Add your action code here.
    }    
}
